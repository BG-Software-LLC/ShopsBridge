group 'Hooks:ExcellentShop4_8'

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(17))
    }
}

repositories {
    maven { url 'https://repo.bg-software.com/repository/common/' }
}

dependencies {
    compileOnly 'su.nightexpress.nexshop:ExcellentShop:4.8.6'
    compileOnly 'su.nexmedia.engine:NexEngine:2.2.10'
    compileOnly "com.bgsoftware.common.reflection:ReflectionUtils:1.0.2"
}
